schema_version = "1.4"
artifacts = []

[task]
name = "maintainops/integrity-error-on-duplicate-sku"
version = "1.0.0"
description = "A duplicate SKU crashes POST /parts/ with a raw 500 instead of a clean JSON error."
keywords = ["error-handling", "fastapi", "sqlalchemy"]
[[task.authors]]
name = "Navyatha"

[metadata]
difficulty = "easy"
category = "bug-fix"
tags = ["backend", "error-handling", "fastapi"]

[verifier]
timeout_sec = 600.0
collect = []

[verifier.env]

[agent]
timeout_sec = 600.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]
