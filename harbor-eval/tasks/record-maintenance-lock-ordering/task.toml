schema_version = "1.4"
artifacts = []

[task]
name = "maintainops/record-maintenance-lock-ordering"
version = "1.0.0"
description = "record_maintenance locks Part rows in client-supplied order instead of a canonical order, risking Postgres deadlocks under concurrency."
keywords = ["concurrency", "deadlock", "sqlalchemy", "postgres"]
[[task.authors]]
name = "Navyatha"

[metadata]
difficulty = "hard"
category = "bug-fix"
tags = ["backend", "concurrency", "sqlalchemy", "postgres"]

[verifier]
timeout_sec = 600.0
collect = []

[verifier.env]

[agent]
timeout_sec = 600.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]
